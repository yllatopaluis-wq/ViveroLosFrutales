@model PagedResult<CotizacionListDto>
@{
    ViewData["Title"] = "Cotizaciones";
    var today = PeruDateTime.Today;
    var firstDay = new DateTime(today.Year, today.Month, 1).ToString("yyyy-MM-dd");
    var currentDay = today.ToString("yyyy-MM-dd");
    var fechaDesde = Context.Request.Query["FechaDesde"].FirstOrDefault() ?? firstDay;
    var fechaHasta = Context.Request.Query["FechaHasta"].FirstOrDefault() ?? currentDay;
    var search = Context.Request.Query["Search"].FirstOrDefault();
    var pageSize = Context.Request.Query["PageSize"].FirstOrDefault() ?? Model.PageSize.ToString();
}

@functions {
    private static bool EsCotizacionConvertida(CotizacionEstado estado) =>
        (int)estado is 3 or 4 or 5;

    private static string EstadoCotizacionTexto(CotizacionEstado estado) => estado switch
    {
        CotizacionEstado.ACTIVA => "Activa",
        CotizacionEstado.CONVERTIDA => "Convertida",
        CotizacionEstado.ANULADA => "Anulada",
        _ => EsCotizacionConvertida(estado) ? "Convertida" : estado.ToString()
    };

    private static string EstadoCotizacionClass(CotizacionEstado estado) => estado switch
    {
        CotizacionEstado.ACTIVA => "success",
        CotizacionEstado.ANULADA => "danger",
        _ => "info"
    };

    private static string RowIconClass(CotizacionEstado estado) => estado switch
    {
        CotizacionEstado.ACTIVA => "success",
        CotizacionEstado.ANULADA => "danger",
        _ => "warning"
    };
}

<section class="comprobantes-page sales-docs-page">
    <form method="get" class="comprobantes-toolbar sales-docs-toolbar">
        <label class="comprobantes-search">
            <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="7" /><path d="m20 20-4-4" /></svg>
            <input name="Search" value="@search" placeholder="Buscar por cliente, cotizaci&oacute;n o documento..." autocomplete="off" />
            @if (!string.IsNullOrWhiteSpace(search))
            {
                <a class="search-clear" asp-action="Index" asp-route-FechaDesde="@fechaDesde" asp-route-FechaHasta="@fechaHasta" asp-route-PageSize="@pageSize" aria-label="Limpiar b&uacute;squeda">&times;</a>
            }
        </label>

        <label class="filter-field">
            <span>Desde</span>
            <input name="FechaDesde" type="date" value="@fechaDesde" />
        </label>

        <label class="filter-field">
            <span>Hasta</span>
            <input name="FechaHasta" type="date" value="@fechaHasta" />
        </label>

        <input type="hidden" name="PageSize" value="@pageSize" />
        <button class="btn-search" type="submit">
            <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="11" cy="11" r="7" /><path d="m20 20-4-4" /></svg>
            Buscar
        </button>

        <button class="btn-export" type="button" id="exportCotizacionesCsv">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 3v12" /><path d="m8 11 4 4 4-4" /><path d="M5 20h14" /></svg>
            Exportar
        </button>
    </form>

    @if (TempData["Error"] is string error)
    {
        <div class="alert alert-danger py-2">@error</div>
    }
    @if (TempData["Success"] is string success)
    {
        <div class="alert alert-success py-2">@success</div>
    }

    <div class="comprobantes-list sales-docs-list" id="cotizacionesList">
        <div class="comprobantes-head sales-docs-head" aria-hidden="true">
            <span>Cotizaci&oacute;n</span>
            <span>Cliente</span>
            <span>Total</span>
            <span>Estado</span>
            <span>Documento</span>
            <span>Acciones</span>
        </div>

        @if (!Model.Items.Any())
        {
            <div class="comprobantes-empty">
                <strong>No hay cotizaciones</strong>
                <span>Ajusta los filtros o registra una nueva cotizaci&oacute;n.</span>
            </div>
        }
        else
        {
            @foreach (var item in Model.Items)
            {
                var numero = $"{item.Serie}-{item.Correlativo}";
                var esEditable = item.EstadoCotizacion == CotizacionEstado.ACTIVA;
                var esAnulable = item.EstadoCotizacion != CotizacionEstado.ANULADA && !EsCotizacionConvertida(item.EstadoCotizacion);
                <article class="comprobante-row sales-docs-row" data-csv="@numero|@item.FechaEmision.ToString("dd/MM/yyyy")|@item.Cliente|@item.Documento|@item.Total.ToString("N2")|@EstadoCotizacionTexto(item.EstadoCotizacion)">
                    <div class="comprobante-main">
                        <span class="comprobante-icon @RowIconClass(item.EstadoCotizacion)" aria-hidden="true">
                            <svg viewBox="0 0 24 24"><path d="M6 3h9l4 4v14H6z" /><path d="M14 3v5h5M9 12h6M9 16h4" /></svg>
                        </span>
                        <span class="comprobante-copy">
                            <strong>@numero</strong>
                            <small>@item.FechaEmision.ToString("dd/MM/yyyy")</small>
                            <small>Cotizaci&oacute;n</small>
                        </span>
                    </div>

                    <div class="comprobante-client">
                        <strong>@item.Cliente</strong>
                        <small>@item.TipoDocumento: @item.Documento</small>
                    </div>

                    <div class="comprobante-total">S/ @item.Total.ToString("N2")</div>

                    <div class="comprobante-status">
                        <span class="pill @EstadoCotizacionClass(item.EstadoCotizacion)">@EstadoCotizacionTexto(item.EstadoCotizacion)</span>
                    </div>

                    <div class="comprobante-sunat">
                        <span class="sunat-line muted">
                            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 3h9l4 4v14H6z" /><path d="M14 3v5h5" /></svg>
                            @item.Documento
                        </span>
                    </div>

                    <div class="comprobante-actions">
                        <details class="row-actions-menu">
                            <summary aria-label="Abrir acciones">
                                <svg viewBox="0 0 24 24" aria-hidden="true"><circle cx="12" cy="5" r="1.5" /><circle cx="12" cy="12" r="1.5" /><circle cx="12" cy="19" r="1.5" /></svg>
                            </summary>
                            <div class="row-actions-panel">
                                <a asp-action="Details" asp-route-id="@item.CotizacionId">
                                    <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6-10-6-10-6Z" /><path d="M12 9a3 3 0 1 1 0 6 3 3 0 0 1 0-6Z" /></svg>
                                    Ver cotizaci&oacute;n
                                </a>
                                <form asp-action="Imprimir" method="post" target="_blank">
                                    @Html.AntiForgeryToken()
                                    <input type="hidden" name="id" value="@item.CotizacionId" />
                                    <button type="submit">
                                        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M6 9V3h12v6" /><path d="M6 18H4a2 2 0 0 1-2-2v-5a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v5a2 2 0 0 1-2 2h-2" /><path d="M6 14h12v8H6Z" /></svg>
                                        Imprimir
                                    </button>
                                </form>
                                @if (esEditable)
                                {
                                    <a asp-action="Edit" asp-route-id="@item.CotizacionId">
                                        <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M12 20h9" /><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z" /></svg>
                                        Editar
                                    </a>
                                    <form asp-action="Convertir" method="post" onsubmit="return confirm('Convertir esta cotizaci&oacute;n a nota de pedido?');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.CotizacionId" />
                                        <button type="submit">
                                            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8Z" /><path d="M14 2v6h6" /><path d="M8 13h7" /><path d="m12 10 3 3-3 3" /></svg>
                                            Convertir a nota
                                        </button>
                                    </form>
                                }
                                @if (esAnulable)
                                {
                                    <form asp-action="Anular" method="post" onsubmit="return confirm('Anular esta cotizaci&oacute;n?');">
                                        @Html.AntiForgeryToken()
                                        <input type="hidden" name="id" value="@item.CotizacionId" />
                                        <button type="submit" class="danger">
                                            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M3 6h18" /><path d="M8 6V4h8v2M19 6l-1 14H6L5 6" /><path d="M10 11v5M14 11v5" /></svg>
                                            Anular cotizaci&oacute;n
                                        </button>
                                    </form>
                                }
                            </div>
                        </details>
                    </div>
                </article>
            }
        }
    </div>

    @await Html.PartialAsync("_Pager", Model)
</section>

<script>
    document.querySelectorAll('.row-actions-menu').forEach(menu => {
        menu.addEventListener('toggle', () => {
            if (!menu.open) return;
            document.querySelectorAll('.row-actions-menu[open]').forEach(other => {
                if (other !== menu) other.removeAttribute('open');
            });
        });
    });

    document.addEventListener('click', event => {
        if (event.target.closest('.row-actions-menu')) return;
        document.querySelectorAll('.row-actions-menu[open]').forEach(menu => menu.removeAttribute('open'));
    });

    document.getElementById('exportCotizacionesCsv')?.addEventListener('click', () => {
        const rows = Array.from(document.querySelectorAll('.sales-docs-row[data-csv]'));
        const header = ['Cotizacion','Fecha','Cliente','Documento','Total','Estado'];
        const csvRows = [header, ...rows.map(row => row.dataset.csv.split('|'))]
            .map(cols => cols.map(value => '"' + String(value ?? '').replaceAll('"', '""') + '"').join(','));
        const blob = new Blob([csvRows.join('\n')], { type: 'text/csv;charset=utf-8;' });
        const url = URL.createObjectURL(blob);
        const link = document.createElement('a');
        link.href = url;
        link.download = 'cotizaciones.csv';
        link.click();
        URL.revokeObjectURL(url);
    });
</script>
